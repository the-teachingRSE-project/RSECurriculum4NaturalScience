\documentclass[
    $if(classoption)$
    $for(classoption)$$classoption$$sep$,$endfor$
    $endif$
]{$documentclass$}

\input{trendsstyle.tex}


\pagestyle{empty}


\newcommand{\stt}{Soft\-ware\-tech\-nik-Trends}


\newcommand{\BI}{\begin{itemize}}


\newcommand{\EI}{\end{itemize}}


\raggedbottom

\usepackage[$if(biblio-style)$style=$biblio-style$,$endif$$for(biblatexoptions)$$biblatexoptions$$sep$,$endfor$]{biblatex}

\usepackage{setspace}
\usepackage{xcolor}
\usepackage{academicons}

$if(bibliography)$
$for(bibliography)$
\addbibresource{$bibliography$}
$endfor$
$endif$


% Standard packages
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}
\usepackage{wrapfig}
\usepackage{float}
\usepackage{colortbl}
\usepackage{pdflscape}
\usepackage{tabularx}
\usepackage{threeparttable}
\usepackage{threeparttablex}
\usepackage[normalem]{ulem}
\usepackage{makecell}

\usepackage{framed} % Needed for code blocks
\usepackage{orcidlink}
\usepackage{authblk}

$if(highlighting-macros)$
$highlighting-macros$
$endif$

% configure hyperlinks
\hypersetup{pdfborder = { 0 0 0 }}

% solve \tightlist error
\providecommand{\tightlist}{%
    \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% solve \pandocbounded error
\providecommand{\pandocbounded}[1]{#1}

$if(build-without-quarto)$
\renewcommand\Authands{\normalfont\small \ and }
\renewcommand\Authfont{\scshape\small}
\renewcommand\Affilfont{\normalfont\footnotesize}
$endif$


% rewrite longtable command to fit 2-columns

\makeatletter
\let\oldlt\longtable
\let\endoldlt\endlongtable
\def\longtable{\@ifnextchar[\longtable@i \longtable@ii}
\def\longtable@i[#1]{\begin{figure}[t]
\onecolumn
\begin{minipage}{0.5\textwidth}
\oldlt[#1]
}
\def\longtable@ii{\begin{figure}[t]
\onecolumn
\begin{minipage}{0.5\textwidth}
\oldlt
}
\def\endlongtable{\endoldlt
\end{minipage}
\twocolumn
\end{figure}}
\makeatother


$if(build-without-quarto)$
\title{$title$}
$for(author)$
  \author[$author.index$]{$author.name$\thinspace\orcidlink{$author.orcid$}\thinspace}
  \affil[$author.index$]{$author.affiliation$\thinspace}
$endfor$
\date{}
$endif$


%stylistic:

% discourage hyphenation
\hyphenpenalty=10000      % almost no hyphenation
\exhyphenpenalty=10000    % no hyphenation at explicit hyphens

% prevent Schusterjungen (club lines, i.e. first line alone at bottom)
\clubpenalty=10000

% prevent Hurenkinder (widow lines, i.e. last line alone at top)
\widowpenalty=10000
\displaywidowpenalty=10000  % for equations

\begin{document}
$if(build-without-quarto)$
    \maketitle
$else$
\input{titlepage}
$endif$

% Body
    $body$


\printbibliography

\end{document}





